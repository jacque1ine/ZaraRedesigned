import { createApp } from 'vue'
import App from './App.vue' //importing root app component from app.vue

createApp(App).mount('#app') //passing in app compent and mounting it to div #app in the html file
